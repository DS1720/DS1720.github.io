import { ActionContext } from './action-context';

describe('ActionContext', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ActionContext()).toBeTruthy();
  });
});
